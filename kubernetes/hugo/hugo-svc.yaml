apiVersion: v1
kind: Service
metadata:
  name: hugo-app-svc
  labels:
    run: hugo-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    run: hugo-app
